# LS World Docs

Documentation MkDocs Material pour LS World.

## Installation

1. Copier tout le contenu à la racine du repo.
2. Vérifier que `mkdocs.yml` est à la racine.
3. Vérifier que `docs/index.md` existe.
4. Push sur `main`.
5. Settings → Pages → Branch `gh-pages` → `/root`.
